<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.consumers.fastwayadmin">

    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.READ_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.RECEIVE_SMS"
        tools:node="remove" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="AllowBackup"
        tools:replace="android:icon">
        <activity android:name=".SplashAndIntro.IntroActivity"></activity>
        <activity android:name=".HomeScreen.SupportActivity" />
        <activity android:name=".Chat.DisplayAllAvaialbleChats" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Info.MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity android:name=".DiscountCombo.SelectDishForCombo" />
        <activity
            android:name=".Tables.ChatWithCustomer"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".DiscountCombo.CustomOffer" />
        <activity android:name=".DiscountCombo.ComboAndOffers" />
        <activity android:name=".DiscountCombo.DiscountActivity" />
        <activity android:name=".MenuActivities.CustomDishImageSearch" />
        <activity android:name=".ListViewActivity.MyOrders" />
        <activity android:name=".ListViewActivity.MyAccount" />
        <activity android:name=".MenuActivities.SearchYourDish" />
        <activity
            android:name=".Info.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".Login.AlreadyHavAccount" />
        <activity android:name=".MenuActivities.EditMenu" />
        <activity android:name=".Tables.AddTables" />
        <activity android:name=".MenuActivities.CreateDish" />
        <activity android:name=".MenuActivities.AllMenuDish" />

        <service
            android:name=".FirebaseNotification"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_baseline_home_24" />
        <!--
 Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/defaultContentColor" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".HomeScreen.HomeScreen" />
        <activity android:name=".Info.Info" />
        <activity android:name=".SplashAndIntro.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>